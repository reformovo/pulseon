name: Change checks

on:
  push:
    branches:
      - main
      - master
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality-gates:
    name: Quality gates
    uses: ./.github/workflows/release-gates.yml
    with:
      full: false
