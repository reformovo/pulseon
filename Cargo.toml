[package]
name = "pulseon"
version = "0.1.0"
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
name = "pulseon"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.28.3", features = ["experimental-inspect"] }
