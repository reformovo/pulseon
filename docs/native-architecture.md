# PulseOn Architecture Redirect

The old broad architecture draft was removed during v1 scope convergence. This
file is only a redirect for older links.

Use these documents instead:

- `docs/v1-native-architecture.md` for the completed v1 native architecture.
- `docs/catalog-data-boundary.md` for the current catalog/data boundary.
- `docs/TODO.md` for the current v2 roadmap.
- `docs/glossary.md` for product terms.
- `docs/adr/` for accepted decisions.
